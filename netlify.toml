[build]
  command = "yarn run dist:styleguides && yarn run dist:storybook && yarn run dist:website"
  publish = "dist/website"


[build.environment]
  NODE_VERSION = "8"
  YARN_VERSION = "1.9.2"
  YARN_FLAGS = "--frozen-lockfile"
