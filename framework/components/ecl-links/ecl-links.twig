{#
  - variant: can be '','external','standalone', 'inverted', 'all', 'more' (default: '')
  - href: target of the link (default: '')
  - label: label of the link (default: '')
#}

{# Internal properties #}

{% set _cssClass = 'ecl-link' ~ (variant != '' ? ' ecl-link--' ~ variant: '') %}
{% set _extraAttributes = '' %}

{# Internal logic - Process properties #}

{% if extraClass is defined %}
  {% set _cssClass = _cssClass ~ ' ' ~ extraClass %}
{% endif %}

{% if extraAttributes is defined %}
  {% for attr in extraAttributes %}
    {% set _extraAttributes = _extraAttributes ~ ' ' ~ attr.name ~ '="' ~ attr.value ~ '"' %}
  {% endfor %}
{% endif %}

{# Print result #}
<a class="{{ _cssClass }}" {{ _extraAttributes }} href="{{href}}">{{label}}</a>
