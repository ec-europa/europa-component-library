global:
  type: all
  category: button

# Define aliases
aliases:
  _ACCORDION_INNER_SPACING: "{!ACCORDION_INNER_SPACING}"

# Export properties
props:

  # Plain state
  BUTTON_DEFAULT_COLOR:
    value: "{!_ACCORDION_INNER_SPACING}"
    comment: "Default accordion inner spacing"
    type: "spacing"
