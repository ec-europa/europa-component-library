import specs from '@ecl/ec-specs-page-header-core/demo/data--title';
import dataLong from '../../ec-component-breadcrumb-core/demo/data';
import adapter from '../adapter';

specs.breadcrumb = dataLong;

export default adapter(specs);
