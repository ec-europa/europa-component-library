# Assign values to the paramters exposed by the button component
aliases:
  BUTTON_DEFAULT_COLOR: "{!COLOR_PRIMARY}"
  BUTTON_DEFAULT_BORDER_COLOR: "{!COLOR_PRIMARY}"
  BUTTON_DEFAULT_BACKGROUND_COLOR: "#00" # just to show that we can put whatever we want
  BUTTON_HORIZONTAL_INNER_SPACING: "{!SPACING_S}"
  BUTTON_VERTICAL_INNER_SPACING: "{!SPACING_XS}"

# Import component
imports:
  - ../../../../ec-component/ec-component-button/tokens.yml
