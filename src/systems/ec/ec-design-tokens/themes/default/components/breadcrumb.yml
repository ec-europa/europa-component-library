global:
  type: all
  category: breadcrumb

# Define aliases
aliases:
  BREADCRUMB_SEGMENTS_WRAPPER_VERTICAL_INNER_SPACING: "{!SPACING_S}"
  BREADCRUMB_LINK_INNER_SPACING: "{!SPACING_XS}"

imports:
  - ../../../../ec-component/ec-component-breadcrumb/tokens.yml
