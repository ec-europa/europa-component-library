# EDS Example (placeholder)

Not a real EDS component — exists only to exercise the Storybook addon
wiring (code view, notes, preview width, dark mode) until a real component
lands. Safe to delete once one does.

### Example

<!-- prettier-ignore -->
```twig
{% include '@ecl/example/example.html.twig' with {
  eyebrow: 'EDS example',
  title: 'Dummy component',
  body: 'Placeholder body text'
} %}
```
