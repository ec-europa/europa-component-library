{#
  Parameters:
  - variant: (string),
  - title: (string),
  - close: (object)
  - description (text)
#}

{% extends "@ecl/@compliance/@checker.html.twig" %}

{% set component = 'message' %}
{% set message = '' %}
{% set no_title_case = title is empty %}
{% set no_description_case = description is empty %}
{% set no_close_button_label_case = close.label is not defined or close.label is empty %}

{% set not_compliant = no_title_case or no_description_case or no_close_button_label_case %}

{% if no_title_case %}
  {% set message = message ~ ' {{ title }} needs to set and filled in.' %}
{% endif %}
{% if no_description_case %}
  {% set message = message ~ ' {{ description }} needs to be set and filled in.' %}
{% endif %}
{% if no_close_button_label_case %}
  {% set message = message ~ ' {{ close.label }} needs to set and filled in, for the close button to be compliant.' %}
{% endif %}
