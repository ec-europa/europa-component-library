---
title: Showcase
order: 2
---

import Playground from '@ecl/website-components/Playground';
import BreadcrumbCore, {
  BreadcrumbCoreItem,
} from '@ecl/ec-react-component-breadcrumb-core';
import BreadcrumbStandardised, {
  BreadcrumbStandardisedItem,
} from '@ecl/ec-react-component-breadcrumb-standardised';
import BreadcrumbHarmonised, {
  BreadcrumbHarmonisedItem,
} from '@ecl/ec-react-component-breadcrumb-harmonised';

import simpleContentCore from '@ecl/ec-specs-breadcrumb-core/demo/data--simple';
import longContentCore from '@ecl/ec-specs-breadcrumb-core/demo/data';
import simpleContentStandardised from '@ecl/ec-specs-breadcrumb-standardised/demo/data--simple';
import longContentStandardised from '@ecl/ec-specs-breadcrumb-standardised/demo/data';
import simpleContentHarmonised from '@ecl/ec-specs-breadcrumb-harmonised/demo/data--simple';
import longContentHarmonised from '@ecl/ec-specs-breadcrumb-harmonised/demo/data';

## Simple breadcrumb

### Core

<Playground
  system="ec"
  selectedKind="components-navigation-breadcrumb-core"
  selectedStory="simple"
>
  <BreadcrumbCore
    label={simpleContentCore.label}
    ellipsisLabel="Click to expand"
  >
    {simpleContentCore.items.map(item => (
      <BreadcrumbCoreItem {...item} key={item.label} />
    ))}
  </BreadcrumbCore>
</Playground>

### Standardised

<Playground
  system="ec"
  selectedKind="components-navigation-breadcrumb-standardised"
  selectedStory="simple"
>
  <BreadcrumbStandardised
    label={simpleContentStandardised.label}
    ellipsisLabel="Click to expand"
  >
    {simpleContentStandardised.items.map(item => (
      <BreadcrumbStandardisedItem {...item} key={item.label} />
    ))}
  </BreadcrumbStandardised>
</Playground>

### Harmonised

<Playground
  system="ec"
  selectedKind="components-navigation-breadcrumb-harmonised"
  selectedStory="simple"
>
  <BreadcrumbHarmonised
    label={simpleContentHarmonised.label}
    ellipsisLabel="Click to expand"
  >
    {simpleContentHarmonised.items.map(item => (
      <BreadcrumbHarmonisedItem {...item} key={item.label} />
    ))}
  </BreadcrumbHarmonised>
</Playground>

## Long breadcrumb

When there are more than 3 items, the breadcrumb should be collapsed by default and items should be displayed if there's enough space.

The first item and the last 2 items should always be visible.

### Core

<Playground
  system="ec"
  selectedKind="components-navigation-breadcrumb-core"
  selectedStory="long"
  showFrame
>
  <BreadcrumbCore
    label={longContentCore.label}
    ellipsisLabel="Click to expand"
    data-ecl-auto-init="BreadcrumbCore"
  >
    {longContentCore.items.map(item => (
      <BreadcrumbCoreItem {...item} key={item.label} />
    ))}
  </BreadcrumbCore>
</Playground>

### Standardised

<Playground
  system="ec"
  selectedKind="components-navigation-breadcrumb-standardised"
  selectedStory="long"
  showFrame
>
  <BreadcrumbStandardised
    label={longContentStandardised.label}
    ellipsisLabel="Click to expand"
    data-ecl-auto-init="BreadcrumbStandardised"
  >
    {longContentStandardised.items.map(item => (
      <BreadcrumbStandardisedItem {...item} key={item.label} />
    ))}
  </BreadcrumbStandardised>
</Playground>

### Harmonised

<Playground
  system="ec"
  selectedKind="components-navigation-breadcrumb-harmonised"
  selectedStory="long"
  showFrame
>
  <BreadcrumbHarmonised
    label={longContentHarmonised.label}
    ellipsisLabel="Click to expand"
    data-ecl-auto-init="BreadcrumbHarmonised"
  >
    {longContentHarmonised.items.map(item => (
      <BreadcrumbHarmonisedItem {...item} key={item.label} />
    ))}
  </BreadcrumbHarmonised>
</Playground>
