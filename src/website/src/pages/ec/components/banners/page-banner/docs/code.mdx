---
title: Showcase
order: 2
---

import Playground from '@ecl/website-components/Playground';
import PageBanner from '@ecl/ec-react-component-page-banner';

import demoContentImage from '@ecl/ec-specs-page-banner/demo/data--image';
import demoContentImageShade from '@ecl/ec-specs-page-banner/demo/data--image-shade';
import demoContentSimple from '@ecl/ec-specs-page-banner/demo/data--simple';

## Image banner

<Playground
  system="ec"
  selectedKind="components-banners-page-banner"
  selectedStory="image"
  showFrame
>
  <PageBanner {...demoContentImage} variant="image" isCentered />
</Playground>

## Shade image banner

<Playground
  system="ec"
  selectedKind="components-banners-page-banner"
  selectedStory="image-shade"
  showFrame
>
  <PageBanner {...demoContentImageShade} variant="image-shade" isCentered />
</Playground>

## Simple banners

<Playground
  system="ec"
  selectedKind="components-banners-page-banner"
  selectedStory="simple-primary"
  showFrame
>
  <PageBanner {...demoContentSimple} variant="primary" />
</Playground>

### Colours

Simple banners can be displayed with several background colours by using one css class or another.
Here are the possibilities

| variant | colour      | css class                   | notes                                                                       |
| ------- | ----------- | --------------------------- | --------------------------------------------------------------------------- |
| primary | blue        | `.ecl-page-banner--primary` | primary colour may vary if a different theme is used                        |
| grey    | light grey  | `.ecl-page-banner--grey`    |                                                                             |
| white   | white       | `.ecl-page-banner--white`   |                                                                             |
| ghost   | transparent | `.ecl-page-banner--ghost`   | ghost variant is intended to be used in a container with a background image |

## Other variants

Note: Variants below can be applied to all kind of banners.  
Check usage guidelines for more information.

### Center content

Adding class `.ecl-page-banner--center` will center the text in the banner.

### Banner width

Banners placed inside a grid container (`.ecl-container`) will expand to this container width.

To have a full width banner, the recommanded way is to place it outside of the grid container.

However, if there is a need to have a full width banner inside the container, the css class `.ecl-page-banner--fullwidth` can be used (just put it at the same level as `.ecl-page-banner`).

**Caution**: if you plan to use full width banner inside a container, we recommand to use the "website" preset, as some global rules here would improve the display.
