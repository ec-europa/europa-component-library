{% apply spaceless %}

{#
  Parameters:
    - "autoinit" (boolean) (default: false) Initialize the JS datepicker automatically
    - "id" (string) (default: random) Unique id for the input
    - "name" (string) (default: same as id) Name attribute for the input
    - "default_value" (string) (default: '') Pre-filled value in YYYY-MM-DD format
    - "width" (string) (default: 'm') Width size of the input; can be 's', 'm', 'l'
    - "required" (boolean) (default: false) Makes the field required
    - "disabled" (boolean) (default: false) Disables the input field
    - "min" (string) (default: '') Minimum selectable date in YYYY-MM-DD format
    - "max" (string) (default: '') Maximum selectable date in YYYY-MM-DD format
    - "first_day_of_week" (integer) (default: 1) First day of the week (0=Sunday, 1=Monday, etc.)
    - "invalid" (boolean) (default: false) Displays the field in invalid state
    - "placeholder" (string) (default: 'DD-MM-YYYY') Placeholder text for the input
    - "extra_classes" (string) (default: '') Extra classes (space separated)
    - "extra_attributes" (array) (default: []) Extra attributes for the datepicker
      - "name" (string) Attribute name, eg. 'data-test'
      - "value" (string) Attribute value, eg: 'data-test-1'
#}

{# Internal properties #}

{% set _autoinit = autoinit|default(false) %}
{% set _css_class = 'ecl-datepicker' %}
{% set _invalid = invalid|default(false) %}
{% set _disabled = disabled|default(false) %}
{% set _required = required|default(false) %}
{% set _default_value = default_value|default('') %} {# YYYY-MM-DD #}
{% set _id = id|default('ecl-datepicker-' ~ random()) %}
{% set _name = name|default(_id) %}
{% set _min = min|default('') %}
{% set _max = max|default('') %}
{% set _width = width|default('m') %}
{% set _first_day_of_week = first_day_of_week|default(1) %}
{% set _placeholder = placeholder|default('DD-MM-YYYY') %}
{% set _extra_attributes = '' %}

{# Internal logic - Process properties #}

{% set _css_class = _css_class ~ ' ecl-datepicker--' ~ _width %}

{% if extra_classes is defined and extra_classes is not empty %}
  {% set _css_class = _css_class ~ ' ' ~ extra_classes %}
{% endif %}

{% if _invalid %}
  {% set _css_class = _css_class ~ ' ecl-datepicker--invalid' %}
  {% set _label_class = _label_class ~ ' ecl-form-label--invalid' %}
  {% set _input_class = _input_class ~ ' ecl-text-input--invalid' %}
{% endif %}

{% if _disabled %}
  {% set _helper_class = _helper_class ~ ' ecl-help-block--disabled' %}
{% endif %}

{% if extra_attributes is defined and extra_attributes is not empty and extra_attributes is iterable %}
  {% for attr in extra_attributes %}
    {% if attr.value is defined %}
      {% set _extra_attributes = _extra_attributes ~ ' ' ~ attr.name|e('html_attr') ~ '="' ~ attr.value|e('html_attr') ~ '"' %}
    {% else %}
      {% set _extra_attributes = _extra_attributes ~ ' ' ~ attr.name|e('html_attr') %}
    {% endif %}
  {% endfor %}
{% endif %}

  <div 
    class="{{ _css_class }}"
    {{ _extra_attributes|raw }}
  {% if _autoinit %}
    data-ecl-auto-init="Datepicker"
  {% endif %}
  {% if _invalid %}
    invalid
  {% endif %}
  {% if _default_value is not empty %}
    data-value="{{ _default_value }}"
  {% endif %}
  {% if _placeholder is not empty %}
    data-placeholder="{{ _placeholder }}"
  {% endif %}
    data-ecl-datepicker-toggle
  >
    <duet-date-picker
      identifier="{{ _id }}"
      name="{{ _name }}"
      first-day-of-week="{{  _first_day_of_week }}"
    {% if _min %}
      min="{{ _min }}"
    {% endif %}
    {% if _max %}
      max="{{ _max }}"
    {% endif %}
    {% if _disabled %}
      disabled
    {% endif %}
    {% if _required %}
      required
    {% endif %}
    ></duet-date-picker>
  </div>

{% endapply %}
