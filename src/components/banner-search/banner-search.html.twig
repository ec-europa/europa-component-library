{% apply spaceless %}

{#
  Parameters:
    - "id" (string) (default: random) Unique id for the component
    - "title" (string) (default: '') Title of search banner
    - "description" (string) (default: '') Description of search banner
    - "search_input" (object) (default: {}) Search field, following ECL Text Input structure.
    - "search_label" (string) (default: '') Search label; for screen readers; falls back to the placeholder if not provided
    - "search_button" (object) (default: {}) Search button, following ECL Button structure
    - "suggestion_label" (string) (default: '') Label for the suggestions
    - "suggestion" (object) (default: {}) List of tags, following the ECL Tag Set structure
    - "extra_classes" (string) (default: '')
    - "extra_attributes" (optional) (array) (default: [])
      - "name" (string) Attribute name, eg. 'data-test'
      - "value" (optional) (string) Attribute value, eg: 'data-test-1'
#}

{# Internal properties #}

{% set _css_class = 'ecl-banner-search' %}
{% set _extra_attributes = '' %}

{% set _id = id|default('ecl-banner-search-' ~ random()) %}
{% set _title = title|default('') %}
{% set _description = description|default('') %}
{% set _search_input = search_input|default({}) %}
{% set _search_label = search_label|default('') %}
{% set _search_button = search_button|default({}) %}
{% set _suggestion_label = suggestion_label|default({}) %}
{% set _suggestion = suggestion|default({}) %}

{# Internal logic - Process properties #}

{% if extra_classes is defined and extra_classes is not empty %}
  {% set _css_class = _css_class ~ ' ' ~ extra_classes %}
{% endif %}

{% if extra_attributes is defined and extra_attributes is not empty and extra_attributes is iterable %}
  {% for attr in extra_attributes %}
    {% if attr.value is defined %}
      {% set _extra_attributes = _extra_attributes ~ ' ' ~ attr.name|e('html_attr') ~ '="' ~ attr.value|e('html_attr') ~ '"' %}
    {% else %}
      {% set _extra_attributes = _extra_attributes ~ ' ' ~ attr.name|e('html_attr') %}
    {% endif %}
  {% endfor %}
{% endif %}

{# Print the result #}

<div
  class="{{ _css_class }}"
  {{ _extra_attributes|raw }}
>
  <div class="ecl-banner-search__content ecl-container">
    {# Title #}
    {% if _title is not empty %}
      <div class="ecl-banner-search__title">
        {{- _title -}}
      </div>
    {% endif %}

    {# Description #}
    {% if _description is not empty %}
      <div class="ecl-banner-search__description ecl-content-color">
        {{- _description -}}
      </div>
    {% endif %}

    {# Input field and button #}
    <form
      class="ecl-banner-search__form"
      role="search"
      {% if _title is not empty %}
        aria-label="{{ _title }}"
      {% endif %}
    >
      {% if _search_input is not empty %}
        {% include '@ecl/icon/icon.html.twig' with {
          icon: {
            name: 'magnifying-glass',
            family: 'phosphor',
            size: 'm',
          },
          extra_classes: 'ecl-banner-search__form-icon'
        } only %}

        {% include '@ecl/form-group/form-group.html.twig' with {
          label: _search_label|default(_search_input.placeholder|default('')),
          hide_label: true,
          extra_classes: 'ecl-banner-search__form-group',
          input: _search_input|merge({
            width: 'l',
            input_type: 'search',
            extra_classes: 'ecl-banner-search__form-input' ~ ( _search_input.extra_classes ? ' ' ~ _search_input.extra_classes : '' ),
          }),
        } only %}

        {% include '@ecl/button/button.html.twig' with _search_button|merge({
          extra_classes: 'ecl-banner-search__form-button'
        }) only %}
      {% endif %}
    </form>

    {# Suggestion #}
    {% if _suggestion is not empty %}
      {% set _suggestion_label_id = _id ~ '-suggestion-label' %}
      <div class="ecl-banner-search__suggestion">
        {% if _suggestion_label is not empty %}
          <span class="ecl-banner-search__suggestion-label" id="{{ _suggestion_label_id }}">
            {{- _suggestion_label -}}
          </span>
        {% endif %}

        {% include '@ecl/tag/tag-set.html.twig' with _suggestion|merge({
          extra_classes: 'ecl-banner-search__suggestion-list',
          extra_attributes: _suggestion_label is not empty ? [{ name: 'aria-labelledby', value: _suggestion_label_id }] : [],
        }) only %}
      </div>
    {% endif %}
  </div>
</div>

{% endapply %}
