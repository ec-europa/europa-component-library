{% apply spaceless %}

{#
  Parameters:
    variant: (string) (default: story) story or testimonial
    - "items" (array) (default: []): List of story cards
      - "id" (string): Unique identifier for the story card
      - "image" (object): Picture component object with img properties
      - "teaser_label" (string): Card title
      - "description" (string): Card description
      - "title" (string) Title of the details section
      - "icon" (object) Object of type icon
      - "source" (string) Source of the item
      - "role" (string) Role of the author
      - "card_link" (object): Read more link
        - "label" (string): Button label
        - "path" (string): Button link path
    - "color_mode" (string) (default: ''): Color mode name
    - "sr_description" (string) (default: ''): Screen reader description
    - "sr_prev" (string) (default: 'Previous card'): Previous button label
    - "sr_next" (string) (default: 'Next card'): Next button label
    - "sr_play" (string) (default: 'Play story cards'): Play button label
    - "sr_pause" (string) (default: 'Pause story cards'): Pause button label
    - "sr_expanded" (string) (default: 'Expand'): Expand button label
    - "sr_collapsed" (string) (default: 'Collapse'): Collapse button label
    - "extra_classes" (string) (default: ''): Extra CSS classes
    - "extra_attributes" (array) (default: []): Extra HTML attributes
#}

{# Internal properties #}

{% set _css_class = 'ecl-story-card' %}
{% set _color_mode = color_mode|default('') %}
{% set _variant = variant|default('story') %}
{% set _extra_attributes = '' %}
{% set _items = items|default([]) %}
{% set _sr_description = sr_description|default('') %}
{% set _sr_prev = sr_prev|default('Previous card') %}
{% set _sr_next = sr_next|default('Next card') %}
{% set _sr_play = sr_play|default('Play story cards') %}
{% set _sr_pause = sr_pause|default('Pause story cards') %}
{% set _sr_expanded = sr_expanded|default('Expand') %}
{% set _sr_collapsed = sr_collapsed|default('Collapse') %}

{# Internal logic - Process properties #}

{% if _color_mode is not empty %}
  {% set _css_class = _css_class ~ ' ' ~ 'ecl-color-mode--' ~ _color_mode %}
{% endif %}

{% if _variant == 'testimonial' %}
  {% set _css_class = _css_class ~ ' ecl-story-card--testimonial' %}
{% endif %}

{% if extra_classes is defined and extra_classes is not empty %}
  {% set _css_class = _css_class ~ ' ' ~ extra_classes %}
{% endif %}

{% if extra_attributes is defined and extra_attributes is not empty and extra_attributes is iterable %}
  {% for attr in extra_attributes %}
    {% if attr.value is defined %}
      {% set _extra_attributes = _extra_attributes ~ ' ' ~ attr.name|e('html_attr') ~ '="' ~ attr.value|e('html_attr') ~ '"' %}
    {% else %}
      {% set _extra_attributes = _extra_attributes ~ ' ' ~ attr.name|e('html_attr') %}
    {% endif %}
  {% endfor %}
{% endif %}

{# Print the result #}

{% if _items is not empty and _items is iterable %}
<div
  class="{{ _css_class }}"
{% if _items|length > 1 %}
  data-ecl-auto-init="StoryCard"
{% endif %}
{% if _sr_description %}
    aria-label="{{ _sr_description }}"
{% endif %}
  {{- _extra_attributes|raw -}}
>
  <!-- Mobile: Embla Carousel -->
  <div
    class="ecl-story-card__carousel ecl-story-card__carousel--mobile"
  >
    <div
      class="ecl-story-card__carousel-viewport"
      data-ecl-story-card-viewport
    >
      <div
        class="ecl-story-card__carousel-container"
        data-ecl-story-card-container
      >
        {% for item in _items %}
          <div
            class="ecl-story-card__carousel-slide"
            data-ecl-story-card-slide
          >
            <div class="ecl-story-card__card">
              {% if item.picture is not empty %}
                <div class="ecl-story-card__image-wrapper">
                  {% include '@ecl/picture/picture.html.twig' with {
                    picture: item.picture,
                    extra_classes: 'ecl-story-card__picture',
                  } only %}
                </div>
              {% endif %}
              <div
                class="ecl-story-card__content"
              >
                {% if _variant == 'testimonial' %}
                  <div class="ecl-story-card__content-icon">
                    <svg xmlns="http://www.w3.org/2000/svg" width="22" height="45" viewBox="0 0 22 45" fill="none">
                      <path d="M0 0H22V23.3143L0 44.5714V0Z" fill="currentColor"/>
                    </svg>
                    <svg xmlns="http://www.w3.org/2000/svg" width="22" height="45" viewBox="0 0 22 45" fill="none">
                      <path d="M0 0H22V23.3143L0 44.5714V0Z" fill="currentColor"/>
                    </svg>
                  </div>
                {%- endif -%}
                {%- if item.title -%}
                  <div class="ecl-story-card__title">
                    {{- item.title -}}
                  </div>
                {%- endif -%}
                {%- if item.author or item.role or item.source -%}
                <div class="ecl-story-card__testimonial">
                  {%- if item.author -%}
                    <span class="ecl-story-card__author">{{- item.author -}}</span>
                  {% endif %}
                  {%- if item.role -%}
                    <span class="ecl-story-card__role">{{- item.role -}}</span>
                  {% endif %}
                  {%- if item.source -%}
                    <span class="ecl-story-card__source">{{- item.source -}}</span>
                  {% endif %}
                </div>
                {%- endif -%}
                {%- if item.description -%}
                  <div class="ecl-story-card__description">{{- item.description -}}</div>
                {% endif %}
                {% if item.card_link %}
                  {% include '@ecl/link/link.html.twig' with item.card_link|merge({
                    type: 'standalone',
                    extra_classes: 'ecl-story-card__link',
                  }) only %}
                {% endif %}
              </div>
            </div>
          </div>
        {% endfor %}
      </div>
    </div>

    <!-- Carousel Controls -->
  {% if _items|length > 1 %}
    <div class="ecl-story-card__pager">
      <button
        class="ecl-story-card__prev"
        data-ecl-story-card-prev
      >
        {% include "@ecl/icon/icon.html.twig" with {
          icon: {
            name: 'corner-arrow',
            transform: 'rotate-270',
            size: 's',
            style: 'monochrome',
          }
        } %}
        <span class="ecl-story-card__prev-label">
          {{ _sr_prev }}
        </span>
      </button>
      <div class="ecl-story-card__dots"></div>
      <button
        class="ecl-story-card__next"
        data-ecl-story-card-next
      >
        <span class="ecl-story-card__next-label">
          {{ _sr_next }}
        </span>
        {% include "@ecl/icon/icon.html.twig" with {
          icon: {
            name: 'corner-arrow',
            transform: 'rotate-90',
            size: 's',
            style: 'monochrome',
          }
        } %}
      </button>
      <script type="text/template" id="ecl-story-card__dot-template">
        <button class="ecl-story-card__dot"></button>
      </script>
    </div>
    {% endif %}
  </div>

  <!-- Desktop: Grid with Accordion -->
  <div class="ecl-story-card__grid ecl-story-card__grid--desktop">
    <div class="ecl-story-card__grid-container">
      {% for item in _items %}
        {% set item_class = 'ecl-story-card__grid-item' %}
        {% if loop.first %}
          {% set item_class = item_class ~ ' ' ~ 'ecl-story-card__grid-item--expanded' %}
        {% endif %}
        <div
          class="{{ item_class }}"
          data-ecl-story-card-grid-item="{{ loop.index0 }}"
        >
          <button
            class="ecl-story-card__grid-card"
            data-ecl-story-card-grid-button="{{ loop.index0 }}"
            aria-label="{{ item.title }}"
            aria-expanded="{{ loop.first ? "true" : "false" }}"
            type="button"
          >
            {% if item.picture is not empty %}
              <div class="ecl-story-card__grid-image">
                {% include '@ecl/picture/picture.html.twig' with {
                  picture: item.picture,
                  extra_classes: 'ecl-story-card__picture',
                } only %}
              {% if _items|length > 1 %}
                <div class="ecl-story-card__grid-loading-bar">
                  {% set _completion = ((loop.index0 + 1) / _items|length) * 100 %}
                  <div
                    class="ecl-story-card__grid-loading-bar-completion"
                    style="width: {{ _completion|default(0) }}%;"
                  ></div>
                </div>
              {% endif %}
              </div>
            {% endif %}
            <div class="ecl-story-card__grid-title-wrapper">
              {% if item.teaser_label %}
                <div class="ecl-story-card__grid-title">{{ item.teaser_label }}</div>
              {% endif %}
            </div>
          </button>
        </div>
      {% endfor %}
    </div>
    {% for item in _items %}
    <div
      class="ecl-story-card__grid-details"
      data-ecl-story-card-grid-details="{{ loop.index0 }}"
      {% if not loop.first %}
        hidden
      {% endif %}
    >
      <div class="ecl-story-card__grid-details-content">
      {% if _variant == 'testimonial' %}
        <div class="ecl-story-card__content-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="22" height="45" viewBox="0 0 22 45" fill="none">
            <path d="M0 0H22V23.3143L0 44.5714V0Z" fill="currentColor"/>
          </svg>
          <svg xmlns="http://www.w3.org/2000/svg" width="22" height="45" viewBox="0 0 22 45" fill="none">
            <path d="M0 0H22V23.3143L0 44.5714V0Z" fill="currentColor"/>
          </svg>
        </div>
      {%- endif -%}
      {%- if item.title -%}
        <div class="ecl-story-card__title">
          {{- item.title -}}
        </div>
      {%- endif -%}
      {% if item.author or item.role or item.source %}
        <div class="ecl-story-card__testimonial">
          {%- if item.author -%}
            <span class="ecl-story-card__author">{{- item.author -}}</span>
          {% endif %}
          {%- if item.role -%}
            <span class="ecl-story-card__role">{{- item.role -}}</span>
          {% endif %}
          {%- if item.source -%}
            <span class="ecl-story-card__source">{{- item.source -}}</span>
          {% endif %}
        </div>
      {% endif %}
      {%- if item.description -%}
        <div class="ecl-story-card__description">{{- item.description -}}</div>
      {% endif %}
        {% if item.card_link %}
          {% include '@ecl/link/link.html.twig' with item.card_link|merge({
            type: 'standalone',
            extra_classes: 'ecl-story-card__link',
          }) only %}
        {% endif %}
      </div>
    </div>
    {% endfor %}
    {% if _items|length > 1 %}
      <div class="ecl-story-card__grid-controls">
        {% include '@ecl/button/button.html.twig' with {
          type: 'button',
          variant: 'tertiary',
          style: 'neutral',
          label: _sr_prev,
          hide_label: true,
          icon: {
            name: 'caret-left',
            size: 'l',
            family: 'phosphor',
          },
          extra_classes: 'ecl-story-card__grid-prev',
          extra_attributes: [{
            name: 'data-ecl-story-card-grid-prev',
          }],
        } only %}

        {% include '@ecl/button/button.html.twig' with {
          type: 'button',
          variant: 'secondary',
          style: 'neutral',
          label: _sr_play,
          hide_label: true,
          icon: {
            name: 'play-filled',
            size: 'l',
            family: 'phosphor',
          },
          extra_classes: 'ecl-story-card__grid-play',
          extra_attributes: [{
            name: 'data-ecl-story-card-grid-play',
          }],
        } only %}

        {% include '@ecl/button/button.html.twig' with {
          type: 'button',
          variant: 'secondary',
          style: 'neutral',
          label: _sr_pause,
          hide_label: true,
          icon: {
            name: 'pause-filled',
            family: 'phosphor',
            size: 'l',
          },
          extra_classes: 'ecl-story-card__grid-pause',
          extra_attributes: [{
            name: 'data-ecl-story-card-grid-pause',
          }],
        } only %}

        {% include '@ecl/button/button.html.twig' with {
          type: 'button',
          variant: 'tertiary',
          style: 'neutral',
          label: _sr_next,
          hide_label: true,
          icon: {
            name: 'caret-right',
            size: 'l',
            family: 'phosphor',
          },
          extra_classes: 'ecl-story-card__grid-next',
          extra_attributes: [{
            name: 'data-ecl-story-card-grid-next',
          }],
        } only %}
      </div>
    {% endif %}
  </div>
</div>
{% endif %}

{% endapply %}
